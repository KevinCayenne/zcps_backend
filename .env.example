# Environment Variables - Secrets Only
#
# This file documents the secrets required for the Django application.
# Non-secret configuration is now managed in config/settings/*.py files.
#
# To use: Copy this file to .env and fill in your actual secret values.

# Django Secret Key
# REQUIRED: Django's secret key for cryptographic signing
# Generate a new key with: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=your-secret-key-here-change-in-production

# REQUIRED: PostgreSQL Database Configuration
# Format: postgres://username:password@localhost:5432/database_name
# Example for local development: DATABASE_URL=postgres://myuser:mypassword@localhost:5432/django_auth_db
# 1. Install PostgreSQL 16: brew install postgresql@16
# 2. Start service: brew services start postgresql@16
# 3. Create database: createdb django_auth_db
# 4. Create user: createuser -P myuser (will prompt for password)
# 5. Grant permissions: psql -c "GRANT ALL PRIVILEGES ON DATABASE django_auth_db TO myuser;"
DATABASE_URL=postgres://username:password@localhost:5432/database_name

# Google OAuth Credentials
# REQUIRED for Google OAuth login feature
# Obtain from: https://console.cloud.google.com/
# 1. Create a project or select existing project
# 2. Enable Google+ API (or Google Identity Services)
# 3. Go to "Credentials" and create "OAuth 2.0 Client ID"
# 4. Add authorized redirect URI: http://localhost:8000/auth/google/callback/ (dev)
#    or https://api.yourdomain.com/auth/google/callback/ (production)
GOOGLE_OAUTH_CLIENT_ID=your-google-client-id-here
GOOGLE_OAUTH_CLIENT_SECRET=your-google-client-secret-here

# Email Configuration (for production SMTP)
# REQUIRED for sending emails in production (password resets, 2FA codes, etc.)
# For Gmail: Use an App Password (not your regular password)
# For other providers: Use your SMTP credentials
EMAIL_HOST_PASSWORD=your-email-password-or-app-password-here

# Email Configuration (Optional overrides)
# These have sensible defaults in settings files, but can be overridden if needed
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your-email@gmail.com

# Production Security (Override in production)
# ALLOWED_HOSTS=yourdomain.com,api.yourdomain.com
